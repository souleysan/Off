import game.Tours;
import java.util.Scanner;
public class Main {
    
    static Scanner nJ = new Scanner(System.in);
    static Scanner nC = new Scanner(System.in);

    public void partie() {

        while (!(Tours.joueur[0].deck.packetComplet.isEmpty()) ||
        !(Tours.joueur[1].deck.packetComplet.isEmpty()) ||
        !(Tours.joueur[2].deck.packetComplet.isEmpty()) ||
        !(Tours.joueur[3].deck.packetComplet.isEmpty())) {
            int turn =  0;

            System.out.println();
        }
    }
}